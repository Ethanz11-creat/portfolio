---
import type { Locale } from '../lib/i18n';
import { t } from '../lib/i18n';
import LanguageSwitcher from './LanguageSwitcher.astro';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const links = [
  { href: `/${locale}/#projects`, label: t(locale, 'nav.projects') },
  { href: `/${locale}/#blog`, label: t(locale, 'nav.blog') },
  { href: `/${locale}/#about`, label: t(locale, 'nav.about') },
];
---
<header class="nav" id="site-nav">
  <div class="container nav-inner">
    <a href={`/${locale}/`} class="logo">Yiheng<span class="dot">.</span></a>
    <nav class="nav-links">
      {links.map((l) => <a href={l.href}>{l.label}</a>)}
      <LanguageSwitcher locale={locale} />
    </nav>
  </div>
</header>

<script>
  const nav = document.getElementById('site-nav');
  const onScroll = () => {
    if (!nav) return;
    nav.classList.toggle('scrolled', window.scrollY > 24);
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();
  // 非首页（无 Hero 深色区）时直接进入 scrolled 态，避免透明导航压浅色背景
  if (!document.querySelector('.hero')) nav?.classList.add('scrolled');
</script>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    transition: background 0.3s, box-shadow 0.3s, backdrop-filter 0.3s;
  }
  .nav.scrolled {
    background: rgba(247, 245, 237, 0.9);
    backdrop-filter: blur(12px);
    box-shadow: 0 1px 0 var(--line);
  }
  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
  }
  .logo {
    color: var(--ink);
    font-family: var(--font-mono);
    font-size: 1.15rem;
    font-weight: 700;
    letter-spacing: -0.02em;
  }
  .nav.scrolled .logo {
    color: var(--ink);
  }
  .logo .dot {
    color: var(--accent);
  }
  .nav-links {
    display: flex;
    align-items: center;
    gap: 28px;
    font-family: var(--font-mono);
    font-size: 0.78rem;
  }
  .nav-links a {
    color: var(--ink-secondary);
    transition: color 0.2s;
  }
  .nav-links a:hover {
    color: var(--ink);
  }
  .nav.scrolled .nav-links a {
    color: var(--ink-secondary);
  }
  .nav.scrolled .nav-links a:hover {
    color: var(--ink);
  }
  .nav.scrolled :global(.lang-switch a) {
    color: var(--ink-secondary);
  }
  .nav.scrolled :global(.lang-switch a.active) {
    color: var(--accent-deep);
  }
  @media (max-width: 640px) {
    .nav-links {
      gap: 16px;
    }
  }
</style>
